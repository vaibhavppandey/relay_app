name: relay_app
description: "exchange stuff..."

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  supabase_flutter: ^2.12.4
  flutter_dotenv: ^6.0.0
  flutter_screenutil: ^5.9.3
  bloc: ^9.2.0
  meta: ^1.17.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  change_app_package_name: ^1.5.0

flutter:

  uses-material-design: true
